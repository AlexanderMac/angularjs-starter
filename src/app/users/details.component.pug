navbar

spinner(ng-if="$ctrl.isLoading")
.container(ng-if="!$ctrl.isLoading")
  h2 {{ $ctrl.user.name }}
  p Email:&nbsp;
    a(href="mailto:{{ $ctrl.user.email }}") {{ $ctrl.user.email }}
  .buttons.text-right
    a.btn.btn-light(href="#/users") Cancel
    a.btn.btn-primary(href="#/users/{{ $ctrl.user.id }}/edit") Edit
